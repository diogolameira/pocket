/** App-wide configuration. Values here replace what used to be scattered as
 *  literals across components. */

export const APP_NAME = 'Pocket'

/** Locale and currency used by every formatter. */
export const LOCALE = 'en-IE'
export const CURRENCY = 'EUR'

/** How many months the spending-trend chart shows, ending at the selected month. */
export const TREND_MONTHS = 6
